const TransferStatus = {
    PENDING: 'pending',
    CONFIRMED: 'confirmed',
    EXPIRED: 'expired',
    CANCELLED: 'cancelled'
};

const UserStatus = {
    ACTIVE: 'active',
    INACTIVE: 'inactive'
};

module.exports = {
    TransferStatus,
    UserStatus
};